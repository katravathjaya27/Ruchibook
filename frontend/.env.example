# URL of the backend API, including the /api suffix.
#
# Local development (default if this file is absent): http://localhost:5000/api
# Production example (after deploying the backend to Render/Railway/etc.):
VITE_API_URL=http://localhost:5000/api
